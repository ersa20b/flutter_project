import 'package:flutter/material.dart';

class SettengsPage extends StatelessWidget {
  const SettengsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
